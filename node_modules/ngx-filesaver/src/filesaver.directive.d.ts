import { ElementRef, EventEmitter } from '@angular/core';
import { HttpClient, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { FileSaverService } from './filesaver.service';
export declare class FileSaverDirective {
    private el;
    private _FileSaverService;
    private _httpClient;
    method: string;
    http: Observable<HttpResponse<Blob>>;
    query: any;
    header: any;
    url: string;
    fileName: string;
    readonly success: EventEmitter<any>;
    readonly error: EventEmitter<any>;
    constructor(el: ElementRef, _FileSaverService: FileSaverService, _httpClient: HttpClient);
    private getName;
    _click(): void;
}
